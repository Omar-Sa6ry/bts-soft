/**
 * ListConstant
 * 
 * Represents insertion positions for list operations.
 * Used when inserting items before or after a pivot element.
 */
export type ListConstant = 'BEFORE' | 'AFTER';

/**
 * SCORE
 * 
 * A simple constant example representing a scoring key.
 * Can be extended or replaced by an enum for more structured usage.
 */
export const SCORE = 'SCORE';
