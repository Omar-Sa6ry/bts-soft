# Treat peer dependencies as optional by default (don't auto-install them).
# The consumer project must provide its own @nestjs/* versions.
auto-install-peers=false

# Do not fail on peer dependency conflicts - let the consumer decide.
strict-peer-dependencies=false
